{
    "apps": [
        {
            "_comment": "bun is started as a plain process. pm2's own bun mode (interpreter bun) requires() the script, and server.ts has top level awaits, which require() cannot load",
            "name": "kute-server",
            "cwd": ".",
            "script": "bun",
            "args": ["run", "src/server.ts"],
            "interpreter": "none",
            "autorestart": true,
            "max_restarts": 10,
            "restart_delay": 2000,
            "env": {
                "NODE_ENV": "production"
            }
        }
    ]
}
